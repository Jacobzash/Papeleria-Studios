import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import { PapeleriaApp } from "./PapeleriaApp";

ReactDOM.render(
  <React.StrictMode>
    <PapeleriaApp />
  </React.StrictMode>,
  document.getElementById("root")
);
